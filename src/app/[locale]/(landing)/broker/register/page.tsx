import BrokerFromScreen from "@/components/screen/brokerScreen/BrokerFromScreen";

type Props = {};

const page = (props: Props) => {
	return (
		<div>
			<BrokerFromScreen type={1} />
		</div>
	);
};

export default page;
