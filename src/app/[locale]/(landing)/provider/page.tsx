import ProviderScreen from "@/components/screen/providerScreen/ProviderScreen";

type Props = {};

const page = (props: Props) => {
	return (
		<div>
			<ProviderScreen />
		</div>
	);
};

export default page;
