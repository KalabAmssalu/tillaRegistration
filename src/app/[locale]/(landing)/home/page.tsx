import LandingScreen from "@/components/screen/landingScreen/LandingScreen";

type Props = {};

const Landingpage = (props: Props) => {
	return (
		<div>
			<LandingScreen />
		</div>
	);
};

export default Landingpage;
